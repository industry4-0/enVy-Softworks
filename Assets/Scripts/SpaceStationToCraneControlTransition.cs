using UnityEngine;

public class SpaceStationToCraneControlTransition : MonoBehaviour
{

    public void Transition()
    {
        Debug.Log("Transition");
    }
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
